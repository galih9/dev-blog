import { ShortifyContentPipe } from './shortify-content.pipe';

describe('ShortifyContentPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortifyContentPipe();
    expect(pipe).toBeTruthy();
  });
});
